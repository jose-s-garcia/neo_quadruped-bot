fastapi
uvicorn[standard]
pyserial

# Camara: en el Jetson, OpenCV (cv2) YA viene con JetPack y CON soporte GStreamer.
# NO instales opencv-python por pip en el Jetson: trae un build SIN GStreamer y
# rompe el pipeline de la camara. En la PC (sin camara) no hace falta cv2.

# LIDAR (RPLIDAR C1): usa un driver NATIVO propio (lidar.py) que habla el
# protocolo Slamtec por pyserial -> NO hace falta la libreria 'rplidar'
# (era incompatible con el C1 y causaba el flapping del descriptor).
